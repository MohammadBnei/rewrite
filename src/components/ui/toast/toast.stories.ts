import ToastStory from './ToastStory.svelte';

export default {
	title: 'UI/Toast',
	component: ToastStory
};

const Template = () => ({
	Component: ToastStory
});

export const Primary = Template.bind({});
